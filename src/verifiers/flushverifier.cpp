#include "verifiers/flushverifier.h"

FlushVerifier::FlushVerifier(){
    this->rank = 5;
}

/*
 *  Verifies that a given Hand is a flush. That is: any 5 cards in the hand have the same suit.
 *  @param hand is a instance of Hand
 *  @returns {true} iff the hand contains any 5 cards with the same suit.
 */
void FlushVerifier::verifyHand(const Hand &hand) {

}
