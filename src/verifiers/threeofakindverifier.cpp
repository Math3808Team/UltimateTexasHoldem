#include "verifiers/threeofakindverifier.h"

ThreeOfAKindVerifier::ThreeOfAKindVerifier(){
    this->rank = 3;
}

/*
 *  Verifies that a given Hand contains 3 cards with the same face value (regardless of suit)
 *  @param hand is a instance of Hand
 *  @returns {true} iff the hand contains 3 of a kind.
 */
void ThreeOfAKindVerifier::verifyHand(const Hand &hand) {

}
